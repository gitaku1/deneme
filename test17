import docling
print("Docling version:", getattr(docling, "__version__", "No version attr"))

# Gerçek yükleme testi
from docling.document import Document
print("Document import OK")


import subprocess, sys

cmd = [sys.executable, "-c", "import docling; from docling.document import Document; print('OK')"]
result = subprocess.run(cmd, capture_output=True, text=True)

print("Return code:", result.returncode)
print("STDOUT:", result.stdout)
print("STDERR:", result.stderr)
